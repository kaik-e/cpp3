<Page x:Class="ForgeMacro.Views.DetectionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Detection"
      Background="{StaticResource SecondaryBrush}"
      Foreground="{StaticResource TextPrimaryBrush}">
    
    <Grid Background="{StaticResource SecondaryBrush}" Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Controls -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" 
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" 
                CornerRadius="12" Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="🎯 Real-time Detection" FontSize="14" FontWeight="Bold" />
                
                <StackPanel Orientation="Horizontal">
                    <Button Content="Capture &amp; Analyze" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" />
                    <Button Content="Start Live Feed" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" />
                    <Button Content="Stop" Style="{StaticResource PrimaryButtonStyle}" Background="{StaticResource DangerColor}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Detection Results -->
        <Border Grid.Row="1" Background="{StaticResource PrimaryBrush}" 
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" 
                CornerRadius="12" Padding="20">
            <StackPanel>
                <TextBlock Text="📊 Detection Results" FontSize="14" FontWeight="Bold" />
                
                <TextBlock Text="No detections yet. Click 'Capture &amp; Analyze' to start." 
                           Foreground="{StaticResource TextSecondaryBrush}" 
                           TextWrapping="Wrap" />

                <!-- Detected Objects List -->
                <ListBox Background="Transparent" BorderThickness="0" 
                         Foreground="{StaticResource TextPrimaryBrush}">
                    <ListBoxItem Foreground="{StaticResource TextSecondaryBrush}" FontSize="12">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⛏️" />
                            <TextBlock Text="Iron Ore" FontWeight="Bold" />
                            <TextBlock Text="(Confidence: 0.92)" Foreground="{StaticResource SuccessColor}" />
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>
            </StackPanel>
        </Border>
    </Grid>
</Page>
